import json
import os.path

from behave.log_capture import capture
import requests


from sys import *

def before_all(context):
    """Perform setup before the first event."""
    #print(context)
    #print(context.config.userdata)
    #print(context.config.userdata["x"])
    pass
